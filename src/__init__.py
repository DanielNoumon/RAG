"""Top-level package for RAG helper modules.

This package exposes the core and retrieval modules under src/. Scripts
import them via `from core import ...` or `from retrieval import ...` after
inserting src/ into sys.path.
"""
